{
  "name": "transactional-email-providers",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "validate:data": "ajv validate -s schemas/providers.schema.json -d src/data/providers.json --strict=false && ajv validate -s schemas/out-of-scope.schema.json -d src/data/out-of-scope.json --strict=false && node scripts/validate-data.mjs",
    "test:ui": "playwright test"
  },
  "dependencies": {
    "astro": "^6.3.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@tailwindcss/vite": "^4.3.0",
    "@types/node": "^22.15.30",
    "ajv-cli": "^5.0.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^6.0.3"
  }
}
